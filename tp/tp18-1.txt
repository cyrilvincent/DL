Deep Learning
=============
1- Appliquer un MLP sur le jeux de données ESC-50 (hidden_layer_sizes=(30,30,30))
2- Estimer le nombre de sample nécessaire pour avoir un réseau performant
3- Lire esc-us.csv et déterminer la category par le nom du fichier
4- Déterminer le nombre de nouveau sample
    @property
    def shortname(self):
        short = self.name.split("_")[0]
        if self.target == 12:
            short = "fire"
        elif self.target == 14:
            short = "birds"
        elif self.target == 23:
            short = "breath"
        elif self.target == 30:
            short = "knock"
        elif self.target == 31:
            short = "click"
        elif self.target == 34:
            short = "opening"
        elif self.target == 43:
            short = "horn"
        elif self.target == 46:
            short = "bells"
        elif self.target == 49:
            short = "handsaw"
        return short
5- Facultatif : charger ESC-US (25Go)
6- Ré-entrainer le modèle
7- Améliorer le jeux de données avec les 431 ZCR et augmenter la taille du réseau de neurone et tester